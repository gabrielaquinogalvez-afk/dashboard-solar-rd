services:
  - type: web
    name: dashboard-solar-rd
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    plan: free
    autoDeploy: true
